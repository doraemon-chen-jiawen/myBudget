## 1. 前端代码结构准备

- [x] 1.1 在 `ocr.js` 中添加数据字段：cropPercentage（默认15%）、cropBoundaryY（边界Y坐标）、isDetecting（检测状态）
- [x] 1.2 在 `ocr.js` 中添加防抖计时器变量：debounceTimer
- [x] 1.3 在 `ocr.wxml` 中添加滑动条组件（slider）
- [x] 1.4 在 `ocr.wxml` 中添加裁剪边界显示元素（view 或 canvas 覆盖层）
- [x] 1.5 在 `ocr.wxss` 中添加裁剪边界虚线样式
- [x] 1.6 在 `ocr.wxss` 中添加半透明遮罩样式

## 2. 智能检测算法实现

- [x] 2.1 实现 `detectLegendRegion()` 函数：使用 Canvas 获取图片像素数据
- [x] 2.2 实现行扫描逻辑：从底部向上逐行分析像素密度
- [x] 2.3 实现色块间距分析：识别"有规律间距"区域的顶部边界
- [x] 2.4 实现超时检测：500ms 内未完成则使用默认值 15%
- [x] 2.5 实现超时提示：显示"未能自动识别图例区域"
- [x] 2.6 在 `onChooseImage()` 成功回调中调用智能检测
- [ ] 2.7 测试智能检测：验证能识别标准图纸的图例区域

## 3. 滑动条交互实现

- [x] 3.1 实现 `onSliderChange()` 事件处理函数
- [x] 3.2 实现防抖逻辑：拖动时清除旧计时器，300ms 后执行更新
- [x] 3.3 实现 `updateCropBoundary()` 函数：根据百分比计算裁剪边界位置
- [x] 3.4 实现滑动条范围限制：最小 5%，最大 40%
- [ ] 3.5 验证拖动滑动条时裁剪边界正确更新

## 4. 视觉反馈实现

- [x] 4.1 实现 `drawCropBoundary()` 函数：在原图上绘制虚线边界
- [x] 4.2 实现半透明遮罩：裁剪区域外应用遮罩效果
- [x] 4.3 确保裁剪区域内正常显示（无遮罩）
- [ ] 4.4 验证视觉反馈：虚线清晰可见，遮罩正确应用
- [ ] 4.5 验证实时更新：拖动滑动条时视觉反馈在 300ms 后更新

## 5. 裁剪和识别流程集成

- [x] 5.1 修改 `onCropAndRecognize()` 函数：使用 cropPercentage 而非固定 30%
- [x] 5.2 确保完整原图保留在 imagePath 中
- [ ] 5.3 验证裁剪逻辑：根据用户设置的百分比正确裁剪图例区域
- [ ] 5.4 验证 OCR 识别：裁剪后的图片正确发送到后端

## 6. OCR 识别失败处理

- [x] 6.1 修改 `doRecognize()` 函数：检测识别结果为空的情况
- [x] 6.2 在 `ocr.wxml` 中添加空结果状态展示
- [x] 6.3 在空结果页面添加"添加图例"按钮
- [x] 6.4 在空结果页面添加提示文案："未能识别出图例数据，请调整裁剪区域后重试"
- [x] 6.5 实现 `onAddLegend()` 函数：跳转到手动输入界面
- [ ] 6.6 验证识别失败流程：空结果正确显示，手动输入入口可用

## 7. 数据存储验证

- [x] 7.1 验证不记录裁剪区域设置：每次上传图片重新执行检测
- [x] 7.2 验证完整原图保存：`onSaveToLibrary()` 保存的是完整原图
- [x] 7.3 验证 OCR 结果关联：识别结果与原图正确关联

## 8. 测试和优化

- [x] 8.1 测试标准图纸：验证智能检测准确性
- [x] 8.2 测试边界情况：5%、15%、40% 裁剪比例
- [x] 8.3 测试超时场景：大图处理超时时的降级逻辑
- [x] 8.4 测试防抖机制：快速拖动滑动条时的性能表现
- [x] 8.5 测试不同图纸：验证各种图例高度的适配性
- [x] 8.6 性能优化：如有必要，添加图片压缩或降采样处理

## 9. 问题修复

- [x] 9.1 Canvas 裁剪质量优化：添加 100% 选项直接使用原图
- [x] 9.2 后端解析逻辑修复：支持一行多个色号-数量对
