## 1. 多模块架构搭建（C1 - 前端）

- [x] 1.1 创建 custom-tab-bar 组件目录和文件，实现根据 currentModule 渲染 budget（首页+我的）和 bead（首页+豆仓+图纸+我的）两组 tab
- [x] 1.2 app.json 改为 custom: true，声明所有 tabBar 页面路径（portal、记账2页、拼豆4页）
- [x] 1.3 创建 pages/portal/portal 模块选择页，包含「记账助手」「拼豆助手」两个卡片入口
- [x] 1.4 app.js globalData 新增 currentModule 字段，onLaunch 中从 Storage 读取并初始化
- [x] 1.5 修改 login.js 登录成功跳转逻辑：检查 currentModule，有值 reLaunch 到对应模块首页，无值 reLaunch 到 portal
- [x] 1.6 记账 profile 页新增「切换模块」按钮，点击 reLaunch 到 portal
- [x] 1.7 portal 页面配置隐藏 tabBar（page.json 中设置）
- [x] 1.8 验收测试：登录 → portal 选择模块 → tabBar 正确显示 → 切换模块 → 重新打开直接进入上次选择模块

## 2. 拼豆模块首页骨架（C2 - 前端）

- [x] 2.1 创建 bead/index 首页，包含搜索框、4个快捷入口（图纸图库、图片转像素图纸、豆仓管理、像素编辑器预留）
- [x] 2.2 首页标签快捷筛选栏组件，展示热门标签并支持多标签叠加筛选
- [x] 2.3 首页精选图纸列表占位（暂用静态数据或空列表）
- [x] 2.4 创建 bead/warehouse 豆仓占位页（仅标题，后续填充）
- [x] 2.5 创建 bead/drawing 图纸占位页（仅标题，后续填充）
- [x] 2.6 创建 bead/profile 我的页（头像昵称 + 切换模块入口 + 功能入口列表 + 退出登录）
- [x] 2.7 验收测试：4个 tab 切换正常，首页布局完整，从我的页可切回记账模块

## 3. 图纸图库 + 详情（C3 - 前后端）

- [x] 3.1 后端：创建拼豆路由模块 /api/bead/*，搭建基础 Express 路由骨架
- [x] 3.2 后端：创建 public_drawing 图纸表和标签表 MySQL 建表脚本
- [x] 3.3 后端：实现图纸 CRUD API（列表+详情+标签筛选+搜索+分页）
- [x] 3.4 后端：实现收藏 API（添加收藏+取消收藏+收藏列表）
- [x] 3.5 前端：bead/drawing 图纸列表页，实现尺寸筛选（52/78/104）、标签筛选、搜索、分页懒加载
- [x] 3.6 前端：图纸详情页，展示预览图、像素图纸、尺寸、标签、用料清单
- [x] 3.7 前端：详情页功能按钮（收藏/取消收藏、下载图纸、复制用料清单、核对库存）
- [x] 3.8 验收测试：浏览图纸 → 筛选 → 查看详情 → 收藏 → 下载

## 4. 图片转像素图纸（C4 - 前端）

- [x] 4.1 前端：图片上传页面（拍照/相册选择，JPG/PNG ≤5MB 限制）
- [x] 4.2 前端：Canvas 预处理（正方形裁剪、亮度/对比度调节）
- [x] 4.3 前端：尺寸选择组件（仅 52/78/104 三个选项）
- [x] 4.4 前端：像素化算法实现（像素化 + 颜色量化 + 标准色卡匹配）
- [x] 4.5 前端：结果展示页（像素图纸 + 色号图例 + 用量统计表）
- [x] 4.6 前端：结果操作功能（下载图纸、复制用料清单、保存到我的图纸）
- [x] 4.7 前端：当日转换次数限制展示和超限提示
- [x] 4.8 验收测试：上传图片 → 选尺寸 → 生成像素图纸+图例+清单 → 下载/复制/保存

## 5. 图纸图例 OCR 识别（C5 - 前后端）

- [x] 5.1 后端：集成 node-tesseract-ocr 和 sharp，配置 Tesseract 路径和语言包（chi_sim+eng）
- [x] 5.2 后端：实现 OCR 识别 API（POST /api/bead/ocr/recognize），接收裁剪图片返回结构化文本
- [x] 5.3 后端：实现色号校准逻辑（与系统标准色卡模糊匹配，修正格式大小写）
- [x] 5.4 前端：图例区域裁剪组件（自动裁剪底部图例区域）
- [x] 5.5 前端：OCR 识别流程页面（上传图纸 → 裁剪图例 → 提交识别 → 结果展示）
- [x] 5.6 前端：人工校验页面（修改/删除/补充识别结果）
- [x] 5.7 前端：OCR 结果统一输出格式（与 C4 结果页一致，共用结果展示样式）
- [x] 5.8 验收测试：上传图纸 → 裁剪图例 → OCR 识别 → 人工修正 → 生成用料清单

## 6. 豆仓基础 — 库存 CRUD + 统计范围（C6 - 前后端）

- [x] 6.1 后端：创建库存相关表（bead_inventory、user_stat_config、bead_inventory_log、bead_colors）
- [x] 6.2 后端：实现库存 CRUD API（列表、入库、出库、统计范围设置）
- [x] 6.3 后端：实现库存统计聚合 API（根据已勾选图纸合并用料、计算缺口）
- [x] 6.4 前端：bead/warehouse 豆仓首页（顶部统计卡片 + 库存列表）
- [x] 6.5 前端：手动入库功能（单条/批量入库表单）
- [x] 6.6 前端：手动出库功能（含库存校验 + 二次确认弹窗）
- [x] 6.7 前端：库存统计范围设置（弹窗展示所有图纸 + 勾选操作）
- [x] 6.8 验收测试：入库 → 查看库存 → 勾选图纸 → 查看统计结果

## 7. 豆仓进阶 — 订单 OCR + 预警 + 日志（C7 - 前后端）

- [x] 7.1 后端：实现订单 OCR API（POST /api/bead/ocr/order），支持图片和文本两种输入
- [x] 7.2 后端：实现库存预警设置 API（阈值保存+查询）
- [x] 7.3 后端：实现库存日志 API（全量记录+筛选+分页）
- [x] 7.4 前端：订单 OCR 入库页面（图片上传/文本粘贴 → 识别 → 校验 → 批量入库）
- [x] 7.5 前端：库存预警设置页面（低库存/缺货阈值配置）
- [x] 7.6 前端：库存日志页面（筛选、分页、来源标识）
- [x] 7.7 前端：首页/个人中心库存预警红点展示
- [x] 7.8 验收测试：上传订单 → 识别入库 → 设置预警 → 查看日志

## 8. 拼豆个人中心完善（C8 - 前端）

- [x] 8.1 前端：我的收藏列表页（展示收藏图纸 + 统计开关 + 取消收藏）
- [x] 8.2 前端：转换记录页面（历史转换/识别记录列表 + 下载 + 复制 + 统计状态）
- [x] 8.3 前端：退出登录功能（清空本地登录态 + reLaunch 登录页）
- [x] 8.4 前端：个人中心快捷跳转入口（库存统计设置、库存日志、库存预警）
- [x] 8.5 验收测试：查看收藏 → 查看记录 → 退出 → 回到登录页